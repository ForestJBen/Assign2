To run the project use mvn clean install or mvn clean test. Both will work.
To see performance analysis of project. Have VisualVm open when above commands are run.
The coverage.html is can also be found under the following path target/site/jacoco/index.html. Here it is labeled index.html.
